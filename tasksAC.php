Task A:
Code will display 30 (not 31) as $a is defined before variable $b is increased.


Task D:

1. strpos helps us to find a position of a string in another string; optional argument allows to search starting from exact place in string starting from beginning or only search at end of given string;
2. function may return a position of item if result is positive or boolean value of FALSE if string is not found.
3. function accepts 3 arguments;
4. 2 arguments (string to search in; string to search for) are mandatory; offset argument is optional;
5. Mandatory arguments are strings and optional argument should be integer;
